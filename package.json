{
  "name": "surph",
  "version": "3.0",
  "main": "dist/run.js",
  "scripts": {
    "compile": "tsc --build --clean && tsc --build && tsc-alias",
    "prod": "yarn compile && NODE_ENV=development && node dist/src/index.js",
    "test": "NODE_ENV=development && ts-node -r tsconfig-paths/register tests/index.ts",
    "dev": "NODE_ENV=development && ts-node -r tsconfig-paths/register src/index.ts"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@types/colors": "^1.2.1",
    "chrono-node": "^2.6.5",
    "colors": "^1.4.0",
    "eris": "^0.17.2",
    "quick.db": "^9.1.7"
  },
  "devDependencies": {
    "@discordjs/opus": "0.5",
    "@sapphire/ts-config": "^4.0.1",
    "@types/node": "^20.5.3",
    "@types/ws": "^8.5.5",
    "better-sqlite3": "^8.5.1",
    "bufferutil": "4.0.7",
    "sodium-native": "3.2",
    "ts-node": "^10.9.1",
    "tsc-alias": "^1.8.7",
    "tsconfig-paths": "^4.2.0",
    "zlib-sync": "0.1"
  }
}
